using UnityEngine;

/// <summary>
/// Marks a root canvas that should keep its own pixel sizing instead of being
/// wrapped and scaled by ResolutionManager's fixed-resolution viewport.
/// </summary>
public sealed class ResolutionCanvasFitOptOut : MonoBehaviour
{
}
