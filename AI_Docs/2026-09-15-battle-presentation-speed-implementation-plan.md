# 전투 행동 연출 배속 구현 계획

1. 배속 선택값(1, 1.5, 2, 3, 4)을 저장·조회하는 설정을 추가한다.
2. 옵션 패널에 저장값과 연결된 드롭다운을 구성한다.
3. 전투 행동 실행기, 유닛 애니메이터, 행동 VFX의 재생 시간만 배율로 보정한다.
4. 설정값의 기본값, 저장, 잘못된 인덱스 복구, 시간 보정 동작을 EditMode 테스트로 검증한다.
