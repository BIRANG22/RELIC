# Do List

1. 문서는 반드시 `AI_Docs` 폴더 내부에만 작성한다.
2. 구현 또는 파일 수정 전에는 조사 결과와 권장 설계를 설명하고 사용자 승인을 받는다.
3. 사용자가 `진행해`, `구현해`, `그대로 해`, `좋아` 등으로 승인하면 해당 요청 범위의 구현이 승인된 것으로 간주한다.
4. 최초 구현 승인은 설계 문서 작성, 구현 계획 작성, 코드 수정, 프리팹·씬 수정, 테스트 작성, 검증까지 포함하며 추가 승인을 요청하지 않는다.
5. 승인된 설계와 작업 범위가 크게 변경되지 않는 한 완료까지 연속해서 진행한다.
6. 불필요한 구현이나 요청 범위를 벗어난 작업은 진행하지 않는다.
7. 기본적으로 현재 체크아웃된 브랜치와 현재 작업 공간에서 작업한다.
8. 커밋, Push, PR, 브랜치 생성·전환, worktree 생성·삭제는 반드시 사용자 승인을 받은 뒤 진행한다.
9. 모든 답변은 한국어로 한다.
10. 테스트는 반드시 `Assets/Tests/EditMode~/` 또는 `Assets/Tests/PlayMode~/` 아래에만 작성한다.
11. Unity 에디터는 항상 열려 있다고 가정하며 batchmode 테스트는 시도하지 않는다.
12. 작업 시작 전 프로젝트 규칙 문서 또는 `AGENTS.md`를 확인한다.
13. 전투 핵심 로직은 멀티플레이를 고려하여 `Command -> State Change -> Result/Event` 구조를 우선하고, UI/VFX/사운드와 분리한다.
14. 상태 변경은 Scene Object보다 `RuntimeId`, `CharacterId`, `MonsterId`, `GridIndex`, `SkillId` 등 안정적인 ID를 우선 사용한다.
15. 전투 결과에 영향을 주는 랜덤은 seed 기반 또는 `BattleRandom`을 사용한다.
16. UI, VFX, 사운드는 결과를 계산하지 않고 결과 이벤트만 재생한다.
17. 완료 보고 시 멀티플레이 구조 영향 여부를 짧게 언급한다.

# Do Not List

1. 최초 구현 승인 없이 코드나 파일을 수정하지 않는다.
2. 승인 후 설계 문서 작성, 구현 계획 작성, 실제 구현 단계마다 다시 승인을 요청하지 않는다.
3. 현재 브랜치, 현재 작업 공간, 일반 체크아웃 여부, worktree 사용 여부를 이유로 작업을 중단하거나 질문하지 않는다.
4. 사용자 허락 없이 커밋, Push, PR, 브랜치 생성·전환, worktree 생성·삭제를 진행하지 않는다.
5. 문서를 `AI_Docs` 외부에 작성하지 않는다.
6. 테스트를 `Assets/Tests/EditMode~/` 또는 `Assets/Tests/PlayMode~/` 외부에 작성하지 않는다.
7. UI에서 전투 핵심 상태를 직접 변경하지 않는다.
8. VFX, 애니메이션, 사운드 완료 여부가 전투 결과를 결정하게 만들지 않는다.
9. 전투 핵심 로직이 UI, Camera, Prefab, Scene 구조에 강하게 의존하게 만들지 않는다.
10. 전투 판정에서 `Time.time`, `Time.deltaTime`, `UnityEngine.Random`을 직접 사용하지 않는다.
11. 전역 mutable static state를 전투 상태 저장소처럼 사용하지 않는다.
12. 네트워크 프레임워크를 전투 핵심 로직에 직접 추가하지 않는다.

# Implementation Rules

1. 작업 순서는 다음을 따른다.

   `규칙 확인 → 코드 조사 → 권장 설계 제시 → 사용자 승인 → 설계 문서 작성 → 구현 → 검증 → 완료 보고`

2. 설계 문서 작성과 구현 계획 작성은 구현 과정의 일부이며 별도 승인 대상이 아니다.
3. 사용자가 `진행해`라고 승인하면 설계 문서 작성부터 실제 구현과 검증까지 모두 승인한 것으로 처리한다.
4. 승인받은 설계를 실질적으로 변경해야 하거나 요청 범위를 크게 벗어나야 하는 경우에만 다시 승인을 받는다.

# Multiplayer Boundary Rules

1. 전투 결과를 변경하는 코드는 UI, VFX, Camera와 분리한다.
2. 새 전투 로직은 `Command -> State Change -> Result/Event` 흐름을 따른다.
3. 동기화 대상은 Scene Object보다 ID 또는 Snapshot을 우선한다.
4. 전투 결과, 보상, 맵 생성, 몬스터 AI 랜덤은 `BattleRandom`을 사용한다.
5. 연출용 랜덤만 `UnityEngine.Random` 사용을 허용한다.
6. 멀티 관련 설계 문서는 `AI_Docs` 내부 문서를 먼저 확인한다.

# Completion Report Rules

완료 보고에는 아래만 포함한다.

- 변경한 파일
- 주요 구현 내용
- 검증 결과
- 검증하지 못한 항목(있다면)
- 멀티플레이 영향 여부
- 커밋/Push/PR 미수행 여부