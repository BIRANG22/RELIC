# Do List

1. 작성하는 문서는 반드시 `AI_Docs` 폴더 내부에만 작성한다.
2. 어떤 작업 또는 구현을 시작하기 전에 반드시 사용자에게 허락을 받는다.
3. 불필요한 구현 작업은 진행하지 않는다.
4. 커밋이나 PR은 반드시 사용자에게 물어보고 진행한다.
5. 모든 답변은 한국어로 한다.
6. 테스트를 생성할 경우 반드시 `Assets/Tests/EditMode~/` 또는 `Assets/Tests/PlayMode~/` 아래에 생성한다.
7. 테스트는 빌드 대상에 포함되지 않도록 반드시 `~`가 붙은 폴더에만 작성한다.
8. Unity 에디터는 항상 열려 있다고 가정한다. 에디터가 열려 있어서 실행할 수 없는 Unity batchmode 테스트는 시도하지 않는다.
9. 작업을 시작하기 전에 프로젝트 규칙 문서 또는 `AGENTS.md` 내용을 먼저 확인한다.
10. 전투, 캐릭터, 몬스터, 스킬, 상태이상, 턴 처리 코드는 추후 멀티플레이 추가를 방해하지 않는 구조로 작성한다.
11. 전투 핵심 로직은 UI, VFX, 사운드, 카메라 연출과 분리한다.
12. 전투 결과는 가능하면 `Command -> 상태 변경 -> 결과 이벤트` 흐름으로 작성한다.
13. 상태 변경에는 scene object 직접 참조보다 안정적인 ID를 우선 사용한다.
    - 예: `RuntimeId`, `CharacterId`, `MonsterId`, `GridIndex`, `SkillId`
14. 랜덤 값이 필요한 경우 seed 기반 또는 결과 기록이 가능한 구조를 우선 고려한다.
15. UI, VFX, 사운드는 전투 결과를 계산하지 않고 결과 이벤트를 보고 재생만 하도록 작성한다.
16. 작업 완료 보고 시 멀티플레이 구조에 영향을 줄 수 있는 변경이 있었다면 짧게 언급한다.

# Do Not List

1. 사용자 허락 없이 구현, 파일 수정, 커밋, PR을 진행하지 않는다.
2. 문서를 `AI_Docs` 외부에 작성하지 않는다.
3. 테스트를 `Assets/Tests/EditMode~/` 또는 `Assets/Tests/PlayMode~/` 외부에 만들지 않는다.
4. `~`가 없는 테스트 폴더를 만들지 않는다.
5. Unity 에디터가 열려 있어서 실패할 가능성이 높은 batchmode 테스트를 반복 시도하지 않는다.
6. 불필요한 리팩토링이나 요청 범위를 벗어난 구현을 하지 않는다.
7. UI 클릭 함수에서 직접 HP, 코스트, 상태이상, 턴 데이터 같은 전투 핵심 상태를 변경하지 않는다.
8. VFX, 애니메이션, 사운드 완료 여부가 전투 결과를 결정하게 만들지 않는다.
9. 전투 핵심 로직이 특정 Canvas, Camera, Scene 배치, Prefab 구조에 강하게 의존하게 만들지 않는다.
10. 전투 판정 로직에서 `Time.time`, `Time.deltaTime`, `Random.Range`에 직접 의존하지 않는다.
11. 전역 static mutable state를 전투 핵심 상태 저장소처럼 남용하지 않는다.
12. 멀티플레이 프레임워크가 정해지기 전에 Mirror, Netcode 등 특정 네트워크 API를 전투 핵심 로직에 직접 섞지 않는다.
13. “일단 로컬에서만 되게”라는 이유로 상태 변경과 연출을 한 함수에 섞지 않는다.

Multiplayer Boundary Rules
1. 전투 결과를 바꾸는 코드는 UI/VFX/Camera 코드와 분리한다.
2. 새 전투 로직은 Command -> State Change -> Result/Event 흐름을 우선한다.
3. 멀티에서 동기화될 가능성이 있는 값은 Scene Object 참조보다 ID/Snapshot을 우선한다.
4. 전투 결과, 보상, 맵 생성, 몬스터 AI 랜덤은 UnityEngine.Random을 직접 쓰지 말고 BattleRandom을 사용한다.
5. UI, VFX, 카메라, 애니메이션 연출 랜덤은 결과에 영향이 없을 때만 직접 Random 사용을 허용한다.
6. 지금은 싱글 플레이 흐름을 유지하고, 네트워크 패키지/서버 권한 구조는 별도 허락 전에는 추가하지 않는다.
7. 멀티 관련 설계 문서는 AI_Docs 내부 문서를 먼저 확인한다.